# News search queries — shared by NewsAPI and GDELT (refreshed daily by optimize.py)
# Format: category | query text
# Categories: samsung, galaxy, ecommerce, smartphone, other (exactly these 5)
# One per line. '#' comments and blank lines ignored. Max 10.
# optimize.py replaces AT MOST 1 query per category per day (not 3 total), and
# for samsung/galaxy/ecommerce/smartphone (not "other") always keeps at least
# 1 query that literally contains the category's own name as a word.
#
# KEEP THESE SHORT — 4 words max, 2-3 preferred. The news APIs AND every term
# together, so extra qualifiers shrink the result set fast: this project's own
# logs show 3-4 word queries pulling 10-16 articles/run while 6-word ones pull
# 0-3. Relevance is filtered later (keyword pre-filter + LLM judge), so a broad
# query with some noise beats a precise one with no results. optimize.py now
# rejects any proposed query over 4 words.

samsung | samsung product launch
samsung | samsung electronics earnings
galaxy | galaxy phone launch
smartphone | smartphone market share
ecommerce | ecommerce trends
ecommerce | ecommerce ai shopping
other | apple intelligence strategy
other | semiconductor memory market trend
other | online retail trends
other | ai search traffic
